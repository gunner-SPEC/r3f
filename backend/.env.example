# Example environment variables for running the backend

# OpenAI API key (optional). If not set the backend will return a stub manifest.
OPENAI_API_KEY=
# Optional: model name. Default: gpt-4o-mini
OPENAI_MODEL=
