# Frontend: simple static UI

This frontend is a minimal single-file UI (frontend/index.html) that sends prompts to the backend /generate endpoint and displays the stubbed manifest.

Run
- Serve the file with a static server or open it directly in the browser.
- The backend must be running at http://localhost:8000 for the default configuration.
